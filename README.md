# Windows Users

If cloning this repository onto a Windows machine, please add the option `-c core.symlinks=true` to your clone command, or run `fix_symlinks.bat` as an administrator after cloning to ensure all symlinks are created correctly.
